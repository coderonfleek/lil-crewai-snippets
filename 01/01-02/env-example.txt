# ─── LLM ─────────────────────────────────────────────────────────────
# Required. Default model across the course is gpt-4o-mini.
OPENAI_API_KEY=sk-...

# Optional overrides. Leave unset to accept defaults.
# MODEL=gpt-4o-mini
# EMBEDDER_PROVIDER=openai
# EMBEDDER_MODEL=text-embedding-3-small

# ─── Storage (Memory + Knowledge, used from Chapter 2 onward) ────────
# Where CrewAI persists memory and knowledge. Safe to leave unset.
# CREWAI_STORAGE_DIR=./.crewai

# ─── Integrations (Chapter 4+) ───────────────────────────────────────
# Flip to "true" to use the AMP enterprise-apps DSL.
# Leave "false" to use the local MCP mock servers (we'll build these in Ch 4).
USE_AMP_APPS=false

# Required only when USE_AMP_APPS=true
# CREWAI_PLATFORM_INTEGRATION_TOKEN=

# Slack channels used by notifications (Chapter 4)
SLACK_CHANNEL_TRIAGE=#support-triage
SLACK_CHANNEL_ESCALATION=#support-escalations
SLACK_CHANNEL_VIP=#support-vip